kind: ConfigMap
apiVersion: v1
metadata:
  name: env-configmap
data:
  # Configuration values can be set as key-value properties
  POSTGRES_HOST: database-1.cakp7q7ljryh.us-east-1.rds.amazonaws.com
  POSTGRES_DB: database-1
  AWS_BUCKET: udagram-001
  AWS_REGION: us-east-1
  JWT_SECRET: postgres 
